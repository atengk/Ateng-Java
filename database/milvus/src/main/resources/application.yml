server:
  port: 15014
spring:
  application:
    name: ${project.artifactId}
---
# Milvus 配置
milvus:
  host: 192.168.1.12
  port: 40140
  database: default
  username: root
  password: Milvus
---
# Spring AI 配置
spring:
  ai:
    openai:
      base-url: https://api.chatanywhere.tech
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini